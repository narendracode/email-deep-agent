[project]
name = "api"
version = "0.1.0"
description = "FastAPI REST service for Email Super Agent"
requires-python = ">=3.12"
dependencies = [
    "common",
    "agent",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[tool.uv.sources]
common = { workspace = true }
agent = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api"]
